export default function Loading() {
  return (
    <main className="min-h-screen grid place-items-center bg-[#F7F6F3]" aria-busy="true" aria-live="polite">
      <p className="animate-pulse text-sm font-medium text-gray-600">Loading BuyerWatch…</p>
    </main>
  )
}
